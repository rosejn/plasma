(ns plasma.route
  (:use [plasma core util connection peer]
        jiraph.graph)
  (:require [logjam.core :as log]
            [lamina.core :as lamina]
            [plasma.query :as q]))

(defn flood-n
  [])

(defn random-walk-n
  [])

(defn greedy-iter
  [])
